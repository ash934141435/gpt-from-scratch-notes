# 第 3 章验收记录

## 覆盖范围

- 视频：22:16–34:57
- 微段：M030–M047，共 18 段
- 字幕：该范围全部标记为“已覆盖”
- 历史候选裁剪复核：15 张，用于原视频事实与时间核对
- 当前正文教学截图：2 张；公开仓库只保留这些实际引用的裁剪图，原始帧不发布
- 代码快照：V2 未训练 Bigram 模型

## 人工核对结论

- `nn.Embedding(65,65)` 已按“当前 token 行 × 下一 token 列”解释，而非泛称语义向量。
- `[B,T,C] → [BT,C]` 与 `[B,T] → [BT]` 的展平顺序和类别维要求均与视频代码一致。
- 均匀猜测基线 `ln(65)≈4.1744` 与视频初始 loss `4.8786` 的差别已说明。
- 生成循环逐项核对了最后时间位置、softmax 维度、multinomial 输出和 cat 时间维。
- 15 张历史候选裁剪的时间均落在所属微段内；正文从中精选 2 张，其余重复静态画面不发布。
- V2 自检覆盖 logits shape、有限 loss、生成长度及起始 token 保留。

## 已知边界

- 本章不提前加入优化器和训练循环，这些属于第 4 章。
- V2 保留教学版全上下文重复计算；Bigram 可只读取最后 token，但不为此增加分支。
