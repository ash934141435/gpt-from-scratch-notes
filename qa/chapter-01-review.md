# 第 1 章验收记录

## 覆盖

- M001–M011 均已形成正文，覆盖 00:00–07:29。
- 对应细粒度字幕事件已在第二轮逐微段复核，并标记为 `已覆盖并人工复核`。
- 章节边界与全局 `timeline.csv` 一致。

## 内容

- 解释了 token、概率生成、语言模型、Transformer、GPT、ChatGPT 和字符级生成。
- 明确了课程会做与不会做的范围。
- 区分了视频 Notebook、`ng-video-lecture` 和 nanoGPT。
- 校正了视频中把 GPT-2 年份说成 2017 的口误，并保持该纠正不影响课程主线。
- 自测覆盖概念、生成流程和判断题；练习可仅用 Python 标准语法运行。

## 截图

- 历史候选裁剪复核：9 张，曾用于核对 ChatGPT 输出、论文、Tiny Shakespeare、生成预览、nanoGPT 和空白代码文件。
- 当前正文教学截图：1 张，只保留字符级生成预览；其余流程与课程地图改用正文和 Mermaid 表达。
- 公开仓库只保留当前正文引用的裁剪图，原始帧和未使用候选图不发布。
- 已删除 8 个在首次抽帧时名称与实际画面不一致的中间文件；它们均可从保留的本地视频重新生成，不影响正文或索引。

## 自动检查

- Markdown 本地链接有效。
- 图片索引文件均存在，标记为正文使用的图片均已链接。
- 全局时间轴连续，字幕无未归属事件。
