# 第 5 章验收记录

## 覆盖范围

- 视频：42:24–79:13
- 微段：M056–M101，共 46 段
- 字幕：该范围全部标记为“已覆盖”
- 历史候选裁剪复核：39 张，用于原视频事实与时间核对
- 当前正文教学截图：7 张；公开仓库只保留这些实际引用的裁剪图，原始帧不发布
- 代码快照：V4 因果前缀平均、V5 带缩放单头 Self-Attention

## 分段修订

初始粗时间轴把矩阵乘法技巧提前了约 4 分钟，标题与实际画面不符。本章制作时重新观看 42:24–58:17，将 M056–M076 改按“玩具张量、循环平均、矩阵化、mask/softmax、注意力过渡”的真实操作边界划分。M086–M093 的已确认样章边界保持不变。

## 人工核对结论

- `[B,T,C]`、前缀切片、下三角矩阵和 batch 矩阵乘法均给出 shape 与最小数字解释。
- 循环、归一化下三角矩阵、mask+softmax 三种实现由 V4 的 `allclose` 自检证明等价。
- token embedding、position embedding、广播相加和 lm_head 的不同维度语义已区分。
- Q/K/V 分别从语法、数学、模型作用和错误后果四个层面解释。
- causal mask、encoder/decoder、self/cross attention、batch 隔离与有向图通信均结合视频原画面说明。
- V5 已加入视频 1:17:18 后介绍的 `1/√head_size` 缩放，避免旧样章代码与完整章节结论不一致。

## 已知边界

- V4/V5 是用于核对关键数学关系的最小演示，不包含训练循环。
- 多头拼接、FeedForward、残差连接、LayerNorm 和 Dropout 从后续章节加入。
