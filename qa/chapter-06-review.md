# 第 6 章验收记录

## 覆盖范围

- 视频：1:19:13–1:24:45
- 微段：M102–M108，共 7 段
- 字幕：该范围全部标记为“已覆盖”
- 历史候选裁剪复核：9 张，用于原视频事实与时间核对
- 当前正文教学截图：1 张；公开仓库只保留这张实际引用的裁剪图，原始帧不发布
- 代码快照：V6 Multi-Head Attention

## 分段修订

初始粗时间轴把 1:19:13–1:19:34 的 Head 封装误归入缩放实验，并把 1:23:46–1:24:45 误写成 FeedForward。逐帧与字幕核对后，前者归入 M102，后者改为多头训练结果及 Transformer 架构图定位；FeedForward 从 M109 开始。

## 人工核对结论

- Head 的完整数据流与上一章数学保持一致，并指出视频变量 C 在多头后应理解为 head size。
- parameter、buffer、普通 tensor 属性的保存、设备移动和梯度行为已区分。
- 注意力在 embedding 与 lm_head 之间的接线位置及错误放置后果已说明。
- 生成窗口裁剪、完整输出保存和 position table 越界之间的因果关系已覆盖。
- `ModuleList`、列表推导、`cat` 与 `stack` 的差异均给出具体错误后果。
- 四个 `[B,T,8]` 拼为 `[B,T,32]` 的 shape 已逐维解释。
- 单头约 2.4、多头约 2.28 的视频结果与能力边界均有说明。
- V6 自检覆盖 buffer 注册、每头 shape、拼接 shape、词表 logits 和超过窗口长度的生成。

## 已知边界

- V6 是结构与生成窗口的最小可运行快照，没有重复执行视频的完整 5000 步训练。
- FeedForward、残差连接、LayerNorm 与输出投影从第 7 章加入。
