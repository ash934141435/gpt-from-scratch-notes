# 第 8 章验收记录

## 覆盖范围

- 视频：1:37:49–1:42:43
- 微段：M129–M136，共 8 段
- 字幕：该范围全部标记为“已覆盖”
- 复核截图：10 张裁剪图；公开仓库仅保留新版正文实际引用的图片，原始帧不随仓库发布
- 代码快照：V10 完整 decoder-only GPT

## 分段修订

逐句核对后，M132–M135 的粗标题存在一段错位：M132 实际仍在解释 Dropout 子网络集成，模型规模化从 M133 才开始；最终 1.48 loss 与硬件建议同属 M135。索引已按实际字幕与画面修正。

## 人工核对结论

- `n_layer/n_head` 参数化与独立 Block 实例的关系已解释。
- Dropout 在 attention 权重、MHA projection 和 FFN 分支末端的三个位置均已覆盖。
- train/eval 模式、inverted dropout 缩放及权重行和不再严格为 1 的隐藏细节已说明。
- batch、block、embedding、head、layer 与学习率的每项影响和资源代价已解释。
- `T²` attention 成本和普通电脑的缩配顺序已明确。
- 视频约 1.48 的 A100 结果没有被误写成跨设备可复现承诺。
- 最终文本的格式学习与语义失败均结合原语料对照说明。
- V10 包含完整训练、loss 与生成路径，并使用本地 Shakespeare 数据执行快速验收。

## 已知边界

- V10 默认缩小为两层 64 维、32 字符上下文并训练 20 步，只用于验证完整路径。
- 复现视频约 1.48 需要视频规模、A100 级硬件和更长训练，不属于自动验收任务。
