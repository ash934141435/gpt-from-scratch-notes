# Self-Attention 初始模板检查（历史记录）

> 本文件记录模板冻结时的验收状态。模板后来已扩展为完整第 5 章；最终状态请看 [第 5 章验收记录](./chapter-05-review.md)。

## 已通过

- 64:41–71:08 连续覆盖，无时间缺口和重叠。
- 8 个微段均包含学习目标、段落任务、语义解释和一句话总结。
- Q/K/V、点积、线性层、transpose、mask、softmax 首次出现均有初学者解释。
- 关键矩阵乘法解释了执行前 shape、执行后 shape、各维含义和错误后果。
- 10 张正文裁剪图全部来自原视频，图注均包含时间、观察位置和含义。
- V5 演示代码包含 shape、行归一化和未来权重为零的可执行断言。
- V5 演示代码已在本地使用 PyTorch 2.13 实际运行，全部断言通过；输出 shape 为 `(2,4,6) -> (2,4,3) / (2,4,4) -> (2,4,3)`。
- 自测覆盖概念、shape 和代码判断；练习包含目标、提示、预期结果和常见错误。

## 有意保留到后续章节

- `1/sqrt(head_size)` 缩放在模板片段之后引入；完整第 5 章与最终 V5 现已覆盖该行。
- Multi-Head、FeedForward、残差和 LayerNorm 属于后续代码版本。
- 全视频 `timeline.csv` 与字幕覆盖表已生成；本文件只保留模板阶段记录。

## 模板冻结前需要读者确认

- 微段密度是否适中；
- 数学解释是否需要更慢或更短；
- 10 张截图对约 6 分钟内容是否过多或不足；
- “删除或改错会怎样”的深度是否达到预期。
